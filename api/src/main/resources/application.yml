spring:
  application:
    name: api
  datasource:
    url: jdbc:postgresql://localhost:5432/ourhub
    username: root
    password: root
    driver-class-name: org.postgresql.Driver
jpa:
  show-sql: true
  open-in-view: false
  hibernate:
    ddl-auto: update
  properties:
    dialect: org.hibernate.dialect.PostgreSQLDialect