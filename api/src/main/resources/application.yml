spring:
  application:
    name: api
  datasource:
    url: jdbc:postgresql:ourhub-db-instantace.cxoi2460emxp.us-east-2.rds.amazonaws.com:5432/ourhub
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
